<?php
    // === ОДНОМЕРНЫЕ МАССИВЫ (способ 1 - array()) ===
    $nums = array(4, 5, 7, 2, 0, -23, 6);
    echo $nums[1] . '<br>';
    $nums[1] = 45;
    echo $nums[1] . '<br>';
    echo $nums[2] . '<br>';


    // === ОДНОМЕРНЫЕ МАССИВЫ (способ 2 - короткий синтаксис []) ===
    $arr = [4, true, 6, "8", 0.4, 'c', 4, 6];
    echo $arr[0] . '<br>';
    $arr[0] = "false";
    echo $arr[0] . '<br>';


    // === АССОЦИАТИВНЫЕ МАССИВЫ (ключ => значение) ===
    $list = [2 => 50, "name" => "Alex", "hobby" => "Footbal"];
    echo $list["name"] . '<br>';
    $list["name"] = "Bob";
    echo $list["name"] . '<br>';
    echo $list[2] . '<br>';


    // === МНОГОМЕРНЫЕ МАССИВЫ (массив массивовCycles) ===
    $matrix = [
        [4, 6.4, 8], 
        [3, 2], 
        [1, 5, 8, "9"]
    ];
    echo $matrix[0][1] . '<br>';
    $matrix[0][1] = 4;
    echo $matrix[0][1];


    // ============================================================
    // ПОДРОБНЫЙ СПРАВОЧНИК ПО ТОМУ, ЧТО БЫЛО В КОДЕ
    // ============================================================
    
    // ──────────────────────────────────────────────────────────────
    // 1. ОДНОМЕРНЫЙ МАССИВ через array()
    // ──────────────────────────────────────────────────────────────
    // $nums = array(4, 5, 7, 2, 0, -23, 6);
    //     Индексы:  0  1  2  3  4   5   6
    // 
    // $nums[1]       // обратились к элементу с индексом 1 → 5
    // $nums[1] = 45  // изменили значение элемента с индексом 1 на 45
    // $nums[2]       // обратились к элементу с индексом 2 → 7
    // ──────────────────────────────────────────────────────────────


    // ──────────────────────────────────────────────────────────────
    // 2. ОДНОМЕРНЫЙ МАССИВ через []
    // ──────────────────────────────────────────────────────────────
    // $arr = [4, true, 6, "8", 0.4, 'c', 4, 6];
    //     Индексы: 0   1    2  3    4    5   6  7
    // 
    // $arr[0]        // обратились к элементу с индексом 0 → 4
    // $arr[0] = "false" // изменили значение элемента с индексом 0 на "false"
    // ──────────────────────────────────────────────────────────────


    // ──────────────────────────────────────────────────────────────
    // 3. АССОЦИАТИВНЫЙ МАССИВ
    // ──────────────────────────────────────────────────────────────
    // $list = [2 => 50, "name" => "Alex", "hobby" => "Footbal"];
    //     Ключи:  2       "name"           "hobby"
    //     Значения: 50    "Alex"           "Footbal"
    // 
    // $list["name"]  // обратились к элементу по ключу "name" → "Alex"
    // $list["name"] = "Bob"  // изменили значение по ключу "name" на "Bob"
    // $list[2]       // обратились к элементу по ключу 2 → 50
    // ──────────────────────────────────────────────────────────────


    // ──────────────────────────────────────────────────────────────
    // 4. МНОГОМЕРНЫЙ МАССИВ
    // ──────────────────────────────────────────────────────────────
    // $matrix = [
    //     [4, 6.4, 8],     // строка 0 (индексы: 0, 1, 2)
    //     [3, 2],          // строка 1 (индексы: 0, 1)
    //     [1, 5, 8, "9"]   // строка 2 (индексы: 0, 1, 2, 3)
    // ];
    // 
    // $matrix[0][1]   // строка 0, элемент 1 → 6.4
    // $matrix[0][1] = 4  // изменили значение на 4
    // ──────────────────────────────────────────────────────────────


    // ============================================================
    // КРАТКОЕ РЕЗЮМЕ ПО МАССИВАМ ИЗ ПРИМЕРОВ
    // ============================================================
    // 
    // array() и []  →  создают массив
    // $arr[индекс]  →  обращение к элементу по индексу (число)
    // $arr["ключ"]  →  обращение к элементу по ключу (строка)
    // $arr[0] = 10  →  изменение элемента
    // $arr[0][1]    →  обращение к элементу в многомерном массиве
    // 
    // Индексы в массиве начинаются с 0!
    // ============================================================
?>
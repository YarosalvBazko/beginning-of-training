<?php
    // === ЦИКЛ FOR ===
    for($i = 100; $i > 20; $i-=5)
        echo $i . '<br>';


    // === ЦИКЛ WHILE ===
    $i = 0;
    while($i <= 10) {
        echo $i.'<br>';
        $i++;
    }


    // === ЦИКЛ DO WHILE ===
    $i = 100;
    do {
        echo $i. '<br>';
    } while($i < 10);


    // === ОПЕРАТОРЫ В ЦИКЛАХ (break и continue) ===
    for($el = 100; $el > 10; $el /=2) {
        if($el < 15)
            break;
        if($el % 2 == 0)
            continue;
        echo $el. '<br>';
    }


    // === ПЕРЕБОР МАССИВА ЧЕРЕЗ for ===
    $list = [5, 7, 3, 8, "some", 45.7];
    for($i = 0; $i < count($list); $i++)
        echo "Element $i: $list[$i]<br>";


    // === ПЕРЕБОР МАССИВА ЧЕРЕЗ foreach ===
    $list_2 = ["age" => 45, "name" => "Alex", "hobby" => "Footbal"];
    $arr = [5, 6, 8, 9];

    foreach($list_2 as $item => $value) {
        echo "Key: $item. Value: $value.<br>";
    }
    foreach($arr as $value)
        echo "Value: $value.<br>";


    // ============================================================
    // ПОДРОБНЫЙ СПРАВОЧНИК ПО ТОМУ, ЧТО БЫЛО В КОДЕ
    // ============================================================
    
    // ──────────────────────────────────────────────────────────────
    // 1. ЦИКЛ FOR
    // ──────────────────────────────────────────────────────────────
    // for(инициализация; условие; шаг) {
    //     код;
    // }
    // 
    // for($i = 100; $i > 20; $i-=5)
    //     $i = 100   →  стартовое значение
    //     $i > 20    →  условие (пока true, цикл выполняется)
    //     $i-=5      →  шаг (уменьшаем на 5)
    // 
    // Вывод: 100, 95, 90, ... 25
    // ──────────────────────────────────────────────────────────────


    // ──────────────────────────────────────────────────────────────
    // 2. ЦИКЛ WHILE
    // ──────────────────────────────────────────────────────────────
    // while(условие) {
    //     код;
    // }
    // 
    // $i = 0;
    // while($i <= 10) {
    //     echo $i;
    //     $i++;
    // }
    // 
    // Вывод: 0, 1, 2, ... 10
    // ──────────────────────────────────────────────────────────────


    // ──────────────────────────────────────────────────────────────
    // 3. ЦИКЛ DO WHILE
    // ──────────────────────────────────────────────────────────────
    // do {
    //     код;
    // } while(условие);
    // 
    // $i = 100;
    // do {
    //     echo $i;
    // } while($i < 10);
    // 
    // Выполнится 1 раз (даже если условие false)
    // Вывод: 100
    // ──────────────────────────────────────────────────────────────


    // ──────────────────────────────────────────────────────────────
    // 4. ОПЕРАТОРЫ В ЦИКЛАХ: break и continue
    // ──────────────────────────────────────────────────────────────
    // break    →  полностью прерывает выполнение цикла
    // continue →  пропускает текущую итерацию и переходит к следующей
    // 
    // for($el = 100; $el > 10; $el /=2) {
    //     if($el < 15)
    //         break;     // если $el < 15, выходим из цикла
    //     if($el % 2 == 0)
    //         continue;  // если $el четное, пропускаем вывод
    //     echo $el;
    // }
    // 
    // $el: 100 → четное → continue (пропуск)
    // $el: 50  → четное → continue (пропуск)
    // $el: 25  → нечетное → вывод: 25
    // $el: 12.5 → < 15 → break (выход)
    // ──────────────────────────────────────────────────────────────


    // ──────────────────────────────────────────────────────────────
    // 5. ПЕРЕБОР МАССИВА ЧЕРЕЗ for
    // ──────────────────────────────────────────────────────────────
    // $list = [5, 7, 3, 8, "some", 45.7];
    // for($i = 0; $i < count($list); $i++)
    //     echo "Element $i: $list[$i]<br>";
    // 
    // count($list) → 6 (количество элементов)
    // $i = 0, 1, 2, 3, 4, 5
    // 
    // Вывод:
    // Element 0: 5
    // Element 1: 7
    // Element 2: 3
    // Element 3: 8
    // Element 4: some
    // Element 5: 45.7
    // ──────────────────────────────────────────────────────────────


    // ──────────────────────────────────────────────────────────────
    // 6. ПЕРЕБОР МАССИВА ЧЕРЕЗ foreach
    // ──────────────────────────────────────────────────────────────
    // foreach($массив as $значение) {
    //     код;
    // }
    // 
    // foreach($массив as $ключ => $значение) {
    //     код;
    // }
    // 
    // $arr = [5, 6, 8, 9];
    // foreach($arr as $value)
    //     echo "Value: $value.<br>";
    // 
    // Вывод:
    // Value: 5.
    // Value: 6.
    // Value: 8.
    // Value: 9.
    // 
    // $list_2 = ["age" => 45, "name" => "Alex", "hobby" => "Footbal"];
    // foreach($list_2 as $item => $value) {
    //     echo "Key: $item. Value: $value.<br>";
    // }
    // 
    // Вывод:
    // Key: age. Value: 45.
    // Key: name. Value: Alex.
    // Key: hobby. Value: Footbal.
    // ──────────────────────────────────────────────────────────────


    // ============================================================
    // КРАТКОЕ РЕЗЮМЕ ПО ЦИКЛАМ
    // ============================================================
    // 
    // for(инициализация; условие; шаг)   →  когда известно количество итераций
    // while(условие)                     →  когда условие проверяется перед выполнением
    // do { } while(условие)              →  когда нужно выполнить хотя бы 1 раз
    // 
    // break    →  выход из цикла
    // continue →  переход к следующей итерации
    // 
    // foreach($arr as $value)            →  перебор значений массива
    // foreach($arr as $key => $value)    →  перебор ключей и значений
    // ============================================================
?>
<?php
    // === ОБЪЯВЛЕНИЕ ПЕРЕМЕННЫХ ===
    $a = 5;
    $str = "Hello";
    $isWeatherGood = false;


    // === УСЛОВНЫЕ ОПЕРАТОРЫ (if/elseif/else) ===
    if($str != "Hello" || $isWeatherGood) {
        echo 'First';
    }
    else if($a == 5) {
        $res = "Second";
        echo $res;

        if($str == "Hello") {
            echo "<br>Yes";
        }
    } 
    else if($a > 50) {
        echo '$a > 50';
    } else if($a <= 45) {
        echo '$a <= 45';
    } else {
        echo "Else";
    }


    // ============================================================
    // ПОДРОБНЫЙ СПРАВОЧНИК ПО ТОМУ, ЧТО БЫЛО В КОДЕ
    // ============================================================
    
    // ──────────────────────────────────────────────────────────────
    // 1. УСЛОВНЫЕ ОПЕРАТОРЫ
    // ──────────────────────────────────────────────────────────────
    // if(условие) {
    //     код;   // выполнится, если условие истинно
    // } else if(условие2) {
    //     код;   // выполнится, если условие2 истинно
    // } else {
    //     код;   // выполнится, если все условия ложны
    // }
    // ──────────────────────────────────────────────────────────────


    // ──────────────────────────────────────────────────────────────
    // 2. ЛОГИЧЕСКИЕ ОПЕРАТОРЫ
    // ──────────────────────────────────────────────────────────────
    // ||   →   ИЛИ   (истинно, если хотя бы одно условие истинно)
    // &&   →   И     (истинно, если оба условия истинны)
    // 
    // true || true   → true
    // true || false  → true
    // false || false → false
    // 
    // true && true   → true
    // true && false  → false
    // false && false → false
    // ──────────────────────────────────────────────────────────────


    // ──────────────────────────────────────────────────────────────
    // 3. ОПЕРАТОРЫ СРАВНЕНИЯ
    // ──────────────────────────────────────────────────────────────
    // ==    →   равно
    // !=    →   не равно
    // >     →   больше
    // <     →   меньше
    // >=    →   больше или равно
    // <=    →   меньше или равно
    // ──────────────────────────────────────────────────────────────


    // ──────────────────────────────────────────────────────────────
    // 4. ВЛОЖЕННЫЕ УСЛОВИЯ
    // ──────────────────────────────────────────────────────────────
    // Можно ставить if внутри другого if:
    // if($a == 5) {
    //     if($str == "Hello") {
    //         echo "Yes";
    //     }
    // }
    // ──────────────────────────────────────────────────────────────


    // ============================================================
    // КРАТКОЕ РЕЗЮМЕ
    // ============================================================
    // 
    // if(условие) { код; }
    // else if(условие) { код; }
    // else { код; }
    // 
    // ||   →   ИЛИ
    // &&   →   И
    // ==   →   равно
    // !=   →   не равно
    // 
    // Можно вкладывать условия друг в друга
    // ============================================================
?>